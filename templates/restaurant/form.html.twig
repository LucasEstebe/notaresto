{% extends 'base.html.twig' %}

{% block title %}Hello RestaurantController!{% endblock %}

{% block body %}
    <div class="card mt-5">
        <div class="card-body">
            <h2 class="mt-3"><i class="fas fa-utensils"></i>New Restaurant</h2>
            <div class="card-body">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-success">Submit</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
