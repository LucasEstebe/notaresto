<div class="bg-info jumbotron jumbotron-fluid m-0">
    <div class="container">
        <h1 class="display-4 text-white">NotaResto</h1>
    </div>
</div>
<nav class="navbar navbar-expand-lg navbar-light bg-info">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                {% if app.user %}
                {{app.user.email}} <a class="nav-link text-white" href="{{ path('app_logout') }}">Logout</a>
                {% else %}
                    <a class="nav-link text-white" href="{{ path('app_login') }}">Login</a>
                {% endif %}
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="#">Features</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="#">Pricing</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white" href="{{path('app_register')}}" tabindex="-1" >Register</a>
            </li>
        </ul>
    </div>
</nav>
